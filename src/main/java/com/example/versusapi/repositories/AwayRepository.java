package com.example.versusapi.repositories;

import com.example.versusapi.models.Away;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AwayRepository extends JpaRepository<Away, Long> {
}
